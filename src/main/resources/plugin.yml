name: ${project.artifactId}
main: ${project.groupId}.${project.artifactId}
version: ${project.version}
author: TrollyLoki

commands:
  teamwork-challenge:
    permission: teamworkchallenges.use
    aliases: [teamchallenge, teamc]